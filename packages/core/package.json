{
  "name": "@syncstream/core",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@syncstream/types": "workspace:*",
    "@syncstream/players": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
